DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=hr_group_db_dev
DB_USER=root
DB_PASSWORD=
JWT_SECRET=change-this-secret-in-production
PORT=3010

# Proxy to the separate Control Room Planning app (Laravel, own DB) — see
# src/services/planningProxy.service.js. Service account, manager role.
PLANNING_API_URL=https://crm.thara-services.mg/planning/api
PLANNING_API_EMAIL=
PLANNING_API_PASSWORD=
